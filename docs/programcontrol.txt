For testing, we will use JUnit and Mockito depending on if a class is dependent on other classes.
In our design, because UserInterface and ProgramControl are dependent on other classes, Mockito will be used
to ensure unit testing and for FileHandler and Decipher, JUnit will be used to create unit tests. We are
comprehensive testing our code using partition equivalence to ensure total code coverage. These tests will be
written before code is written.

UML -
ProgramControl
--------------
-filehandler: FileHandler
-cipher: Cipher
--------------
+fetchFile(): String
+fetchFile(String fileName): String
+fetchFile(String fileName, String key): String

PSEUDOCODE -

fetchFile() -
    return filehandler.readFile()

fetchFile(fileName) -
    text = filehandler.readFile(fileName)
    return cipher.decipher()

 fetchFile(fileName, key) -
    text = filehander.readFile(fileName)
    return cipher.decipher(key)