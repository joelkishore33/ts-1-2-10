Team Member A(Dennis)

Assignment: You are responsible for the command line interface used by the user to run the program.
Create a file, userinterface.txt, in the docs folder to document the user interface.
(the command prompt options)

User interface:
(User input:1)
    Here is the file content of file filea.txt:
    FILE CONTENT
(User input: )
    Here is a list of available files
    01 filea.txt
    02 fileb.txt
    03 filec.txt
(User input: 4)
    ERROR or Try again with a new file index

Psuedo code:
Get user input from the java main class
if the user does not enter anything(no argument), then return list of available files
if user returns the name of a file, then prompt the file handler to display corresponding files
if they return a number out of bounds, then give an exception or ask for another number
Also check to see if the person has input a second parameter that will be used by the cipher
function to decipher a text

UML:
userInterface
----------------------
-fileSelected
-altKey
----------------------
partitionUserInput()
transferFileSelected()
